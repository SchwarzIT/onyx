---
"sit-onyx": patch
---

fix(OnyxDataGrid:stickyColumn):

- made selection column sticky when both features are active.
- Added the ability to configure sticky positioning (`left` or `right`) individually per column.
