---
"sit-onyx": minor
---

feat: make `OnyxSplitButton` stable

The `OnyxSplitButton` is now considered stable and is therefore renamed from `OnyxUnstableSplitButton` to `OnyxSplitButton`.
Other changes:

- remove `default` slot
- support new properties: `iconPosition`, `type`, `alignment` and `position`
